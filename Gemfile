source "https://rubygems.org"

# This helps ensure the proper Jekyll version is running with GitHub Pages
gem "github-pages", group: :jekyll_plugins, require: false

# Default theme for new Jekyll sites. You may change this to anything you like.
gem "minima", "~> 2.5"

# Plugins for GitHub Pages
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

# Windows-specific gems for better performance and compatibility

# Commenting out http_parser.rb as it is not necessary unless using JRuby
# gem "http_parser.rb", "~> 0.6.0", :platforms => [:jruby]
gem 'webrick', '~> 1.7'
gem 'csv', '~> 3.1'
